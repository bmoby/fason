
<link rel="stylesheet" href="../css/contact.css">
<link rel="stylesheet" href="../css/restylenavbar.css">
<link rel="stylesheet" href="../css/headerpadding.css">
{{#if user}}
  {{> userheader}}
{{else}}
  {{> header}}
{{/if}}

    <div class="contacter-container">
        <form method="post" action="/contacter">
            <h2 class="text-center">Nous contacter</h2>
                <p class="help-block">Nous allons vous répondre le plus rapidement possible.</p>
            <div class="form-group">
                <textarea class="mainLoginInput form-control" rows="14" name="messagemail" placeholder="Message"></textarea>
            </div>
            <div class="form-group">
                <button class="button btn btn-default login-register-btn" type="submit">envoyer</button>
            </div>
        </form>
    </div>
